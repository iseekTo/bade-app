import React from 'react'
import { Layout } from 'antd'

const { Header } = Layout


const HeaderTop = () => {

    return (
        <Layout>
            <Header className="header">
            </Header>
        </Layout>
    )
}

export default HeaderTop
